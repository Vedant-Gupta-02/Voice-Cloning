import VoiceCloneCard from "./components/VoiceCloneCard";

export default function App() {
  return (
    <div className="min-h-screen bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-[#14141a] via-[#0a0a0a] to-black text-white flex items-center justify-center px-6">
      <VoiceCloneCard />
    </div>
  );
}
